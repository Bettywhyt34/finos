// Root route is handled by app/(dashboard)/page.tsx
// This file must not export a React component — doing so would create a
// duplicate route conflict with (dashboard)/page.tsx (both resolve to "/").
export {};
